spring:
  datasource:
    url: jdbc:mysql://mysql_server:${DB_PORT:3306}/${DB_NAME:todo}?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
  data:
    redis:
      host: redis_server
      port: ${REDIS_PORT:6379}
      password: 123456

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl

logging:
  level:
    root: WARN
    com.todo: INFO
    com.todo.mapper: WARN
